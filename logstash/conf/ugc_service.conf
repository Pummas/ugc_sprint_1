input {
    pipeline { address => ugc_service }
}
filter {
    mutate {
        add_field => { "type" => "ugc_service" }
    }
}
output {
    elasticsearch {
        hosts => [ "${ELK_ES_HOST}" ]
        index => "%{[type]}-%{+YYYY.MM.dd}"
    }
}